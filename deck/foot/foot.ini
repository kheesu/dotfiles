[main]
# Disable the kitty keyboard protocol — without this, foot sends escape
# sequences like ;2;27 for tilde and other keys on some systems.
notify-focus-inhibit=no

[keyboard]
kitty-functional-keys=no
